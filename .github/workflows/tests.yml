name: tests

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    container: ghcr.io/astral-sh/uv:python3.14-trixie
    steps:
      - uses: actions/checkout@v4
      - run: ./hooks/test.sh
